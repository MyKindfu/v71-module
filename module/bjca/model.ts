
export interface UIOpts {
  btnLoginSelector: string  // 验证用户名口令的提交按钮
  certIdInputSelector: string // 证书id隐藏输入框
  formSelector: string  // 表单选择器
  pwdInputSelector: string  // 证书口令输入框
  usernameInputSelector: string // 证书用户名（仅用于显示）
}
